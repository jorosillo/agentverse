/**
 * Hook personalizado de autenticación.
 * Proporciona el estado de sesión del usuario actual.
 */

// TODO: Fase 1 — Implementar completamente
// - useAuth(): { user, isLoading, isAuthenticated, logout }

export function useAuth() {
  // Placeholder — será implementado en Fase 1
  return {
    user: null,
    isLoading: true,
    isAuthenticated: false,
  };
}
