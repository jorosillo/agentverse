/**
 * Hook personalizado de onboarding.
 * Gestiona el progreso del checklist y la visualización del modal.
 */

// TODO: Fase 2 — Implementar completamente
// - useOnboarding(): { isComplete, steps, progress, completeStep, skipOnboarding }

export function useOnboarding() {
  // Placeholder — será implementado en Fase 2
  return {
    isComplete: false,
    progress: 0,
    steps: [],
  };
}
