/**
 * Server Actions para moderación (bloqueo/reporte).
 */
'use server';

// TODO: Fase 6 — Implementar completamente
// - blockUser(blockedId: string): Promise<ActionResult>
// - unblockUser(blockedId: string): Promise<ActionResult>
// - reportUser(reportedUserId: string, category: ReportCategory, reason?: string): Promise<ActionResult>

export async function placeholder() {
  return { success: true as const, data: undefined };
}
